import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    toggleContainer:{
      position:'absolute',
      bottom:20,
      right:10
    },
    loadingContainer: {
      justifyContent:'center',
      alignItems:'center',
      flex:1
    },
    noResults :{
      justifyContent:'center',
      alignItems:'center',
      flex:2,
      marginTop:20
    }
  })